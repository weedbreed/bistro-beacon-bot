(ns bistro-beacon-bot.core
  (:require [integrant.core :as ig]
            [dialog.logger :as log])
  (:gen-class))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))
